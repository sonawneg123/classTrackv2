# features

Feature-sliced modules (auth, assignments, users, reports, notifications).
Each feature folder owns its own components, hooks, services, and types —
keeps domain logic isolated from shared architecture.
